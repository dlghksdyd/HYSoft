<Window x:Class="Samples.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Samples"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800"
        x:Name="Root"
        d:DataContext="{d:DesignInstance local:MainViewModel}">
    <Grid>
        <StackPanel>
            <Button Content="파일 전송 테스트" Margin="10" Padding="10" Command="{Binding FileTransferSampleCommand}"></Button>
            <Button Content="팝업 테스트" Margin="10" Padding="10" Command="{Binding ModalSampleCommand}"></Button>
        </StackPanel>
    </Grid>
</Window>
