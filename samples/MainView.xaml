<Window x:Class="Samples.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Samples"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800"
        x:Name="Root"
        d:DataContext="{d:DesignInstance local:MainViewModel}">
    <Grid>
        <DockPanel LastChildFill="False">
            <Button DockPanel.Dock="Top" Content="File Transfer Test" Padding="10" Command="{Binding FileTransferSampleCommand}"></Button>
            <Button DockPanel.Dock="Top" Content="Modal Test" Padding="10" Command="{Binding ModalSampleCommand}"></Button>
        </DockPanel>
    </Grid>
</Window>
