<UserControl x:Class="Samples.Presentation.Modal.ModalInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Samples.Presentation.Modal"
             mc:Ignorable="d" 
             Height="200" Width="400"
             d:DataContext="{d:DesignInstance local:ModalInfoViewModel}">
    <Border x:Name="TopBorder" CornerRadius="8" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="모달 입니다." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40"></TextBlock>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Command="{Binding OkCommand}" Content="확인" Padding="40 12 40 12" Margin="0 0 20 0"></Button>
                <Button Command="{Binding CancelCommand}" Content="취소" Padding="40 12 40 12"></Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
