<Window x:Class="Samples.Presentation.Styles.Icons.IconsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Samples.Presentation.Styles.Icons"
        xmlns:hy="clr-namespace:HYSoft.Presentation.Styles.Icons;assembly=Presentation.Styles"
        mc:Ignorable="d"
        Title="IconsView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Image Width="24" Height="24">
                <Image.Style>
                    <Style TargetType="Image">
                        <!-- 기본 상태 색상 -->
                        <Setter Property="Source"
                                Value="{hy:Icon BluetoothDisabledRegular, ColorKey=SectionPrimaryMenu}" />

                        <!-- Hover 시 색상 교체 -->
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Source"
                                        Value="{hy:Icon BluetoothDisabledRegular, ColorKey=SectionPrimaryContent}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
        </StackPanel>
    </Grid>
</Window>
