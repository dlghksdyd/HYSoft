<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="clr-namespace:HYSoft.Presentation.Interactivity;assembly=Presentation"
    xmlns:c="clr-namespace:HYSoft.Presentation.Styles.ColorTokens"
    xmlns:hy="http://schemas.hysoft.com/wpf/2025/xaml"
    xmlns:local="clr-namespace:HYSoft.Presentation.Styles.Controls">
    <Style TargetType="{x:Type local:HyTitleBar}">
        <Setter Property="Background" Value="{c:Color TitleBarBackground}" />
        <Setter Property="Foreground" Value="{c:Color TitleBarText}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HyTitleBar}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        b:WindowDragBehavior.EnableDragMove="True"
                        Background="{TemplateBinding Background}"
                        IsHitTestVisible="True"
                        SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" Content="{TemplateBinding Content}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <local:HyButton
                                    x:Name="MinimizeButton"
                                    Margin="{TemplateBinding IconMargin}"
                                    Padding="{TemplateBinding IconPadding}"
                                    VerticalAlignment="Center"
                                    BorderThickness="0">
                                    <hy:EventToCommand.Binding>
                                        <hy:Event
                                            Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=MinimizeAppCommand}"
                                            HandledToo="True"
                                            RoutedEvent="{x:Static local:HyButton.MouseLeftButtonUpEvent}" />
                                    </hy:EventToCommand.Binding>
                                    <local:HyButton.Style>
                                        <Style TargetType="local:HyButton">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#33ffffff" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </local:HyButton.Style>
                                    <local:HyIcon
                                        Width="{TemplateBinding IconSize}"
                                        Height="{TemplateBinding IconSize}"
                                        Source="Remove"
                                        Color="{TemplateBinding Foreground}" />
                                </local:HyButton>
                                <local:HyButton
                                    x:Name="ExitButton"
                                    Margin="{TemplateBinding IconMargin}"
                                    Padding="{TemplateBinding IconPadding}"
                                    VerticalAlignment="Center"
                                    BorderThickness="0">
                                    <hy:EventToCommand.Binding>
                                        <hy:Event
                                            Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ExitAppCommand}"
                                            HandledToo="True"
                                            RoutedEvent="{x:Static local:HyButton.MouseLeftButtonUpEvent}" />
                                    </hy:EventToCommand.Binding>
                                    <local:HyButton.Style>
                                        <Style TargetType="local:HyButton">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#33ffffff" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </local:HyButton.Style>
                                    <local:HyIcon
                                        Width="{TemplateBinding IconSize}"
                                        Height="{TemplateBinding IconSize}"
                                        Source="Close"
                                        Color="{TemplateBinding Foreground}" />
                                </local:HyButton>
                            </StackPanel>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>