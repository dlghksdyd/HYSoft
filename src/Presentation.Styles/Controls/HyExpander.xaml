<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:HYSoft.Presentation.Styles.ColorTokens"
    xmlns:local="clr-namespace:HYSoft.Presentation.Styles.Controls">

    <!--  HyExpander Style  -->
    <Style TargetType="{x:Type local:HyExpander}">
        <Setter Property="Background" Value="{c:Color SurfaceBase}" />
        <Setter Property="BorderBrush" Value="{c:Color BorderSubtle}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{c:Color TextPrimary}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HyExpander}">
                    <Border
                        x:Name="OuterBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <DockPanel>
                            <!--  Header (ToggleButton)  -->
                            <ToggleButton
                                x:Name="HeaderToggle"
                                DockPanel.Dock="Top"
                                IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                OverridesDefaultStyle="True"
                                Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border
                                            x:Name="HeaderBorder"
                                            Padding="8"
                                            Background="Transparent">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <!--  Arrow indicator  -->
                                                <Path
                                                    x:Name="Arrow"
                                                    Grid.Column="0"
                                                    Width="10"
                                                    Height="10"
                                                    Margin="0,0,8,0"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0,0 L 6,4 L 0,8 Z"
                                                    Fill="{c:Color TextPrimary}"
                                                    RenderTransformOrigin="0.5,0.5"
                                                    Stretch="Uniform"
                                                    SnapsToDevicePixels="False">
                                                    <Path.RenderTransform>
                                                        <RotateTransform Angle="0" />
                                                    </Path.RenderTransform>
                                                </Path>

                                                <!--  Header content  -->
                                                <ContentPresenter
                                                    Grid.Column="1"
                                                    VerticalAlignment="Center"
                                                    RecognizesAccessKey="True" />
                                            </Grid>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <!--  Expanded: rotate arrow  -->
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Arrow" Property="RenderTransform">
                                                    <Setter.Value>
                                                        <RotateTransform Angle="90" />
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>

                                            <!--  Hover  -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="HeaderBorder" Property="Background" Value="{c:Color SurfaceSubtle}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <ContentPresenter
                                    ContentSource="Header"
                                    RecognizesAccessKey="True" />
                            </ToggleButton>

                            <!--  Content area  -->
                            <Border
                                x:Name="ContentBorder"
                                Visibility="Collapsed">
                                <ContentPresenter
                                    Margin="{TemplateBinding Padding}" />
                            </Border>
                        </DockPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Expanded: show content  -->
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ContentBorder" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  HyGroupBox Style  -->
    <Style TargetType="{x:Type local:HyGroupBox}">
        <Setter Property="Background" Value="{c:Color SurfaceBase}" />
        <Setter Property="BorderBrush" Value="{c:Color BorderSubtle}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{c:Color TextPrimary}" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HyGroupBox}">
                    <Grid SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!--  Header text (offset from top border)  -->
                        <Border
                            Grid.Row="0"
                            Margin="8,-1,0,0"
                            Padding="4,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Background="{TemplateBinding Background}"
                            Panel.ZIndex="1">
                            <ContentPresenter
                                ContentSource="Header"
                                RecognizesAccessKey="True" />
                        </Border>

                        <!--  Content border  -->
                        <Border
                            Grid.Row="0"
                            Grid.RowSpan="2"
                            Margin="0,8,0,0"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}" />
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
