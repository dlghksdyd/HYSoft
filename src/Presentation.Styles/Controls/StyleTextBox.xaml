<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HYSoft.Presentation.Styles">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Style/StyleColor.xaml"></ResourceDictionary>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:StyleTextBox}">
        <Setter Property="Background" Value="{StaticResource style/color/surface/white}"></Setter>
        <Setter Property="WaterMark" Value=""></Setter>
        <Setter Property="WaterMarkForeground" Value="{StaticResource style/color/text/watermark}"></Setter>
        <Setter Property="CornerRadius" Value="4"></Setter>
        <Setter Property="Padding" Value="10"></Setter>
        <Setter Property="Margin" Value="0"></Setter>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        <Setter Property="HorizontalContentAlignment" Value="Left"></Setter>
        <Setter Property="CanKorean" Value="True"></Setter>
        <Setter Property="CanPaste" Value="True"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StyleTextBox}">
                    <Border x:Name="xBorder" Grid.Column="1" CornerRadius="{TemplateBinding CornerRadius}" Background="{TemplateBinding Background}">
                        <Grid>
                            <!-- Watermark -->
                            <TextBlock x:Name="WatermarkText"
                                       Text="{TemplateBinding WaterMark}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Foreground="{TemplateBinding WaterMarkForeground}"
                                       IsHitTestVisible="False"
                                       Visibility="Hidden"
                                       Padding="{TemplateBinding Padding}"
                                       Margin="2 0 2 0"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>

                            <!-- 실제 입력 박스 -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                          FontSize="{TemplateBinding FontSize}"
                                          Background="{StaticResource style/color/surface/transparent}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Padding="{TemplateBinding Padding}"
                                          Margin="0"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          FontWeight="{TemplateBinding FontWeight}"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="WatermarkText" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        
                        <!-- 비활성화: 투명도 낮춤 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="xBorder" Property="Opacity" Value="0.7"/>
                            <Setter TargetName="WatermarkText" Property="Opacity" Value="0.7"/>
                            <Setter TargetName="PART_ContentHost" Property="Opacity" Value="0.7"/>
                            <Setter Property="Cursor" Value="Arrow"></Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>