<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HYSoft.Presentation.Styles.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Themes/StyleColor.xaml"></ResourceDictionary>
        <ResourceDictionary Source="/Themes/StyleFontSize.xaml"></ResourceDictionary>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:StyleCheckBox}">
        <!-- 시스템 브러시는 테마 변경 반영 위해 DynamicResource 권장 -->
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Padding" Value="4,0,0,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="TextPadding" Value="10 0 0 0"></Setter>
        <Setter Property="HoverBackground" Value="{StaticResource Style/Color/Surface/Hover2}"></Setter>
        <Setter Property="PressedBackground" Value="{StaticResource Style/Color/Surface/Hover2}"></Setter>
        <Setter Property="CheckBrush" Value="{StaticResource Style/Color/Surface/Selected}"></Setter>
        <Setter Property="CheckThickness" Value="3"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StyleCheckBox}">
                    <Grid x:Name="Root" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 박스 -->
                        <Border x:Name="Box"
                                Width="{TemplateBinding BoxSize}"
                                Height="{TemplateBinding BoxSize}"
                                Background="{TemplateBinding BoxBackground}"
                                BorderBrush="{TemplateBinding BoxBorderBrush}"
                                BorderThickness="{TemplateBinding BoxBorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="2">
                            <!-- 체크 마크 -->
                            <Path x:Name="CheckGlyph"
                                  Grid.Column="0"
                                  Stretch="Uniform"
                                  Stroke="{TemplateBinding CheckBrush}"
                                  StrokeThickness="{TemplateBinding CheckThickness}"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Data="M 4 10 L 8 14 L 14 6"
                                  Opacity="0"
                                  IsHitTestVisible="False"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Border>

                        <!-- Content -->
                        <ContentPresenter Grid.Column="2"
                                          Margin="{TemplateBinding TextPadding}"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>

                        <!-- Focus 사각형 -->
                        <Rectangle x:Name="FocusRect"
                                   Grid.ColumnSpan="3"
                                   StrokeThickness="1"
                                   StrokeDashArray="1 2"
                                   Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                                   Opacity="0"
                                   IsHitTestVisible="False"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- 체크 상태 -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckGlyph" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- Indeterminate -->
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="CheckGlyph" Property="Data" Value="M 4 10 L 14 10"/>
                            <Setter TargetName="CheckGlyph" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- 마우스오버/프레스: TemplateBinding ❌, TemplatedParent 바인딩 ✅ -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box"
                                    Property="Background"
                                    Value="{Binding HoverBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Box"
                                    Property="Background"
                                    Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>

                        <!-- 포커스 표시 -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="FocusRect" Property="Opacity" Value="0.6"/>
                        </Trigger>

                        <!-- 비활성화 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity"
                                    Value="{Binding DisabledOpacity, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter TargetName="Box" Property="BorderBrush" Value="#FFC9C9C9"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>