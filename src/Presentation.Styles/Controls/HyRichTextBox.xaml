<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:HYSoft.Presentation.Styles.ColorTokens"
    xmlns:local="clr-namespace:HYSoft.Presentation.Styles.Controls">

    <Style TargetType="{x:Type local:HyRichTextBox}">
        <Setter Property="Background" Value="{c:Color InputBg}" />
        <Setter Property="Foreground" Value="{c:Color InputFg}" />
        <Setter Property="BorderBrush" Value="{c:Color InputBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HyRichTextBox}">
                    <Border
                        x:Name="xBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        Focusable="False">
                        <Grid>
                            <TextBlock
                                x:Name="WatermarkText"
                                Margin="2,0,2,0"
                                Padding="{TemplateBinding Padding}"
                                VerticalAlignment="Top"
                                FontSize="{TemplateBinding FontSize}"
                                Foreground="{TemplateBinding WatermarkForeground}"
                                IsHitTestVisible="False"
                                Text="{TemplateBinding Watermark}"
                                Visibility="Hidden" />
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Margin="-2"
                                Padding="{TemplateBinding Padding}"
                                Background="Transparent"
                                FontSize="{TemplateBinding FontSize}"
                                Foreground="{TemplateBinding Foreground}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDocumentEmpty" Value="True">
                            <Setter TargetName="WatermarkText" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="xBorder" Property="Opacity" Value="0.7" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
