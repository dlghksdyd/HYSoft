# FileTransferClient.SendFileAsync method

지정한 파일을 서버로 전송합니다.

```csharp
public Task SendFileAsync(string filePath)
```

| parameter | description |
| --- | --- |
| filePath | 전송할 로컬 파일 경로. |

## Return Value

비동기 작업을 나타내는 Task. 전송 완료 시 정상적으로 완료되며, 서버가 오류를 반환하거나 네트워크 오류 발생 시 IOException 또는 InvalidOperationException이 발생합니다.

## Exceptions

| exception | condition |
| --- | --- |
| ArgumentException | *filePath*가 null 또는 빈 문자열일 경우. |
| FileNotFoundException | 지정된 경로에 파일이 존재하지 않는 경우. |
| IOException | 파일 크기 오류, 서버 오류 응답, 전송 중단 등 입출력 예외가 발생한 경우. |
| InvalidOperationException | 이미 다른 파일 전송 작업이 진행 중인 경우. |

## Remarks

프로토콜 순서:

1. 클라이언트 → 서버: 헤더 [MAGIC_HEADER(4)][Flags(1)][FileSize(8)][NameLen(2)][FileName]
2. 서버 → 클라이언트: 응답 [Status(1)][StartOffset(8)]
3. 클라이언트 → 서버: 파일 데이터 (StartOffset ~ 끝까지)
4. 클라이언트 → 서버: Tail [MAGIC_TAIL(4)][CRC32(4)]
5. 서버 → 클라이언트: 최종 상태 [FinalStatus(1)]

## See Also

* class [FileTransferClient](../FileTransferClient.md)
* namespace [HYSoft.Communication.Tcp.Client.Protocol.FileTransfer](../../Communication.md)

<!-- DO NOT EDIT: generated by xmldocmd for Communication.dll -->
