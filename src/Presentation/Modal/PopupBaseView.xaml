<UserControl x:Class="HYSoft.Presentation.Modal.PopupBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HYSoft.Presentation.Modal"
             mc:Ignorable="d" 
             Height="450" Width="800"
             d:DataContext="{d:DesignInstance local:PopupBaseViewModel}">
    <Grid>
        <ItemsControl ItemsSource="{Binding PopupList}" d:ItemsSource="{d:SampleData ItemCount=1}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid></Grid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Rectangle Fill="{Binding Background}"></Rectangle>
                        <ContentControl Content="{Binding Content}"></ContentControl>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
