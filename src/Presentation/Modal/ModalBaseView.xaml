<UserControl
    x:Class="HYSoft.Presentation.Modal.ModalBaseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HYSoft.Presentation.Modal"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance local:ModalBaseViewModel}"
    mc:Ignorable="d">
    <Grid>
        <ItemsControl d:ItemsSource="{d:SampleData ItemCount=1}" ItemsSource="{Binding PopupList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Rectangle Fill="{Binding Background}" />
                        <ContentControl Content="{Binding Content}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
